import { FontesDirective } from './fontes.directive';

describe('FontesDirective', () => {
  it('should create an instance', () => {
    const directive = new FontesDirective();
    expect(directive).toBeTruthy();
  });
});
